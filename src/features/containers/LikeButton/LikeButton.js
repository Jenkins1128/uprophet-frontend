const LikeButton = ({ likeCount }) => {
	//const [didLike, setDidLike] = useState(false);

	return (
		<div>
			<p>${likeCount} likes</p>
			{/* <button onClick={}>

            </button> */}
		</div>
	);
};

export default LikeButton;
